#include "function_pointers.h"
#include <stdlib.h>
/**
 * array_iterator - executes a function given as parameter on each elemnt of an array
 * @array:
 * @size:
 * @action: 
 * Return: nothing
 */
void array_iterator(int *array, size_t size, void (*action)(int))
{
	
}
